
# Generally ignored files
/release
/node_modules
/tmp

## We will be ignoring a huge chunk of files that are generated during the build; which unfortunately means
## a lot of directory and/or per file references. We don't want em polluting our repository and the build generates em.
# Server side ignores in definitions (generated by gulp tasks)
/definitions/Server/app.d.ts
/definitions/Server/Collection/**/*.d.ts
# Client side ignores in definitions (generated by gulp tasks)
/definitions/Client/index.d.ts
/definitions/Client/Collection/**/*.d.ts